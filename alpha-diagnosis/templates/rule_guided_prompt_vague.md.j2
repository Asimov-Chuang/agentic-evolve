{# Vague rule-guided injection — natural-language factors without fraction thresholds #}

---

## Diagnostic factors for algorithm improvement

Through analysis of high-performing programs, we identified the following {{ rules|length }} behavioral patterns that matter for improving performance. Use them as qualitative design guidance when proposing new algorithms.

**Important:** Treat each pattern below as an independent design principle. Do NOT assume any relative importance or weight among them — focus only on the behavioral intent described.

{% for rule in rules %}
### Factor {{ loop.index }}: {{ rule.name }}
{{ rule.description }}

{% endfor %}
---

## Your task

Based on the current best program ({{ best_attempt_id }} / best_program.py) as the starting baseline, **design and submit exactly {{ rules|length }} new algorithm variants** — one per factor above:

{% for rule in rules %}
{{ loop.index }}. **Algorithm {{ loop.index }}** — primarily targets Factor {{ loop.index }} ({{ rule.name }})
{% endfor %}

For each algorithm:
- Start from the best program logic and make targeted changes that reflect the corresponding factor's intent.
- Each variant should be meaningfully different in how it implements that factor.
- Write to candidate.py, run `python submit.py candidate.py`, and record the score.
- After all {{ rules|length }} are submitted, briefly summarize which factors helped most based on the scores.

Begin with Algorithm 1 now.
