# Quadruped References

`gait_config.json` and `ant.xml` are copied from `Frontier-Engineering/benchmarks/Robotics/QuadrupedGaitOptimization/references` as local fallback assets for the agentic-evolve example.

The evaluator prefers the Frontier checkout assets when available, then falls back to these local copies.
