# Battery Configuration

`battery_config.json` is copied from `Frontier-Engineering/benchmarks/EnergyStorage/BatteryFastChargingSPMe/references/battery_config.json` so this example has a stable local fallback for model parameters.

The evaluator still locates and loads the Frontier benchmark evaluator for candidate validation and official aggregate scoring.
